venv/
__pycache__/
*.pyc
vocab.db
vocab.db-shm
vocab.db-wal
.tmp_parse/
*.json.bak

# macOS
.DS_Store

# 用户私有的测试样本 PDF（项目根的 *.pdf，不入仓库）
/*.pdf

# PyInstaller 构建产物（每次本地构建生成，不入仓库）
build/
dist/
*.spec.bak

# 打包产物中间文件（保留 spec 和 build_assets/icon.icns 源文件，不保留 .iconset 临时目录）
*.iconset/
